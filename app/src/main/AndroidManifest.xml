<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.kbs.pocis">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".welcome.SplashScreen" android:theme="@style/Fullscreen">

        </activity>
        <activity android:name=".welcome.Welcome_Screen" android:theme="@style/Fullscreen"/>
        <activity android:name=".welcome.Login"/>
        <activity android:name=".activity.HomePage"/>
        <activity android:name=".activity.CreateBooking">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".detailboking.BookingDetails"/>
        <activity android:name=".activity.OnlineBook"/>
        <activity android:name=".activity.MyProject_Dasar"/>
    </application>

</manifest>